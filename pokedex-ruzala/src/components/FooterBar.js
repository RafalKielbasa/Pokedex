import { Box } from "@mui/material";

export default function FooterBar() {
  return (
    <Box
      sx={{
        display: "flex",
        backgroundColor: "primary.light",
        textAlign: "center",
        width: "100%",
      }}
    >
      Footer
    </Box>
  );
}
